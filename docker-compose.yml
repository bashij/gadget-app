version: '3'
services:
  db:
    image: mysql:8.0
    env_file: .env
    volumes:
      - db_data:/var/lib/mysql
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/gadget-app
    ports:
      - '3000:3000'
    depends_on:
      - db
volumes:
  db_data: